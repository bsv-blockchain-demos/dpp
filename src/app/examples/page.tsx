'use client';

import { ExamplesList } from "../../components/examples/examplesList";

export default function ExamplesPage() {
    return (
        <main className="mx-auto max-w-[1000px] px-6 py-9 sm:px-10 sm:py-12">
            <ExamplesList />
        </main>
    );
}
